--- sageos_build/kernel/core/sagelang/sageos_bridge.c
+++ sageos_build/kernel/core/sagelang/sageos_bridge.c
@@ -194,7 +194,9 @@
 
     // Otherwise treat as direct code
     dmesg_printf("sage_execute: treating %s as direct code", mod);
+    dmesg_printf("sage_execute: before parse");
     Stmt* program = sage_parse_string(mod);
+    dmesg_printf("sage_execute: after parse");
     dmesg_printf("sage_execute: program loaded, interpreting...");
     if (program) {
         interpret(program, g_sage_env);
