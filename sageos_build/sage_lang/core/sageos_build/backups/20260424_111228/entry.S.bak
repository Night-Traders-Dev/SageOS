.global _start
.extern kmain

.section .text
.code64

_start:
    cld

    lea stack_top(%rip), %rsp
    andq $-16, %rsp

    call kmain

.hang:
    hlt
    jmp .hang

.section .bss
.align 16
stack_bottom:
    .skip 65536
stack_top:
